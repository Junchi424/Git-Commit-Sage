# Git-Commit-Sage 🤖

一个极简的 AI 驱动 Git 提交助手。

### 功能
- 自动提取 `git diff` 变化。
- 利用大模型语义理解能力，自动生成符合规范的 Commit Message。
- 支持一键确认并提交。

### 使用方法
1. 安装依赖：`pip install requests`
2. 在脚本中配置你的 `API_KEY`。
3. 执行 `python commit_sage.py` 即可。